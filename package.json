{
  "name": "acl",
  "version": "1.0.0",
  "description": "lambda that uses an access control list to allow access to a gateway api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "copremesis",
  "license": "ISC",
  "dependencies": {
    "cidr-js": "^2.3.1",
    "faker": "^5.5.3",
    "futoin-ipset": "^1.3.7"
  }
}
